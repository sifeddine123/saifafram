import Affichplate from './Affichplate'
import {plates} from './Plates'

// export default function Funcplate() { 
//     return (
//         plates.map(function (plate,i) { 
//            return  <Affichplate plate={plate} key={i}/>
//          })
//     )
//  }

//==================================================================================

//  export default function Funcplate() { 
//     return (
//         plates.filter(function (plate) { 
//             return plate.ingredient1==='Eggs' || plate.ingredient2 ==='Eggs' || plate.ingredient3==='Eggs' || plate.ingredient4==='Eggs' || plate.ingredient5==='Eggs' || plate.ingredient6==='Eggs'  
//          }).map(function (plate,i){
//             return <Affichplate plate={plate} key={i}/>
//          }
         
//     ))
// } 

//==================================================================================


// export default function Funcplate() { 
//     const props = plates.find(function (plate) {    
//         return plate.plateName === 'Pasta Carbonara'; 
//     });

//     return (
      
//       <Affichplate plate={props} /> 
  
//     );
// }

